# Same Sanity project the web/ build reads from. Reuse the exact values you
# already set as SANITY_PROJECT_ID / SANITY_DATASET in the web/ Cloudflare
# Pages project's environment variables.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
