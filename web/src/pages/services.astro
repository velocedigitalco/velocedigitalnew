---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Services — Veloce Digital" description="What Veloce Digital does.">
  <section class="container stub">
    <p class="eyebrow">Services</p>
    <h1>What we do</h1>
    <p>Full service details are on their way — check back soon.</p>
  </section>
</BaseLayout>

<style>
  .stub {
    padding-block: var(--section-pad);
    max-width: 40rem;
  }

  .stub h1 {
    margin-top: var(--space-2);
    font-size: var(--fs-h2);
    font-weight: 600;
    color: var(--color-paper);
  }

  .stub p:last-child {
    margin-top: var(--space-3);
    font-size: var(--fs-body-lg);
    color: var(--color-steel);
  }
</style>
