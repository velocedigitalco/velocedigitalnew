# Sanity Content Lake - public, read-only, safe to expose per blueprint Section 08
SANITY_PROJECT_ID=
SANITY_DATASET=production

# Only needed once the dataset is set to private
# SANITY_API_READ_TOKEN=
